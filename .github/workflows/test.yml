name: tests
# This script should test and run the program in native environment.

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: windows-latest
            target: x86_64-pc-windows-gnu

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          override: true

      - name: Set environment variables
        run: |
          echo "TARGET=${{ matrix.target }}" >> $GITHUB_ENV

      - name: Show config for debug purposes
        run: cargo tree

      - name: Build and Test on Linux
        if: matrix.target == 'x86_64-unknown-linux-gnu'
        run: |
          cargo build --release
          cargo test

      - name: Build and Test on macOS
        continue-on-error: true
        if: matrix.target == 'x86_64-apple-darwin'
        env:
          RUSTFLAGS: "-C linker=x86_64-apple-darwin19-clang"
        run: |
          cargo build --release
          cargo test

      - name: Build and Test on Windows
        continue-on-error: true
        if: matrix.target == 'x86_64-pc-windows-gnu'
        run: |
          cargo build --release
          cargo test

      - name: Run program on Linux
        if: matrix.target == 'x86_64-unknown-linux-gnu'
        run: |
          timeout 60s ./target/release/ifstat-rs -V
          timeout 60s ./target/release/ifstat-rs --version
          timeout 60s ./target/release/ifstat-rs -a 0 3

      - name: Run program on macOS
        continue-on-error: true
        if: matrix.target == 'aarch64-apple-darwin'
        run: |
          brew install coreutils
          gtimeout 60s ./target/release/ifstat-rs -V
          gtimeout 60s ./target/release/ifstat-rs --version
          gtimeout 60s ./target/release/ifstat-rs -a 0 3

      - name: Run program on Windows
        continue-on-error: true
        if: matrix.target == 'x86_64-pc-windows-gnu'
        run: |
          ./target/release/ifstat-rs.exe -V
          ./target/release/ifstat-rs.exe --version
          ./target/release/ifstat-rs.exe -a 0 3
