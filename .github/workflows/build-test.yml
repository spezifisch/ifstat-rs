name: Build and test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: ubuntu-latest
            target: riscv64gc-unknown-linux-gnu
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
          - os: ubuntu-latest
            target: armv7-unknown-linux-gnueabihf
          - os: ubuntu-latest
            target: i686-unknown-linux-gnu
          - os: macos-latest
            target: x86_64-apple-darwin
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: windows-latest
            target: x86_64-pc-windows-gnu
          - os: windows-latest
            target: aarch64-pc-windows-gnu

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          override: true

      - name: Install target
        run: rustup target add ${{ matrix.target }}

      - name: Install cross
        run: cargo install cross

      - name: Install dependencies on Linux
        if: runner.os == 'Linux'
        run: sudo apt-get install -y gcc-multilib g++-multilib

      - name: Set environment variables
        run: echo "TARGET=${{ matrix.target }}" >> $GITHUB_ENV

      - name: Show config
        run: cargo tree

      - name: Build and Test on Linux
        if: runner.os == 'Linux'
        run: |
          if [ "${{ matrix.os }}" == "ubuntu-latest" ] && [ "${{ matrix.target }}" != "x86_64-unknown-linux-gnu" ]; then
            cross build --release --target ${{ matrix.target }}
            cross test --release --target ${{ matrix.target }}
          else
            cargo build --release --target ${{ matrix.target }}
            cargo test --release --target ${{ matrix.target }}
          fi

      - name: Build and Test on macOS
        if: runner.os == 'macOS'
        run: |
          if [ "${{ matrix.target }}" == "aarch64-apple-darwin" ]; then
            cross build --release --target ${{ matrix.target }}
            cross test --release --target ${{ matrix.target }}
          else
            cargo build --release --target ${{ matrix.target }}
            cargo test --release --target ${{ matrix.target }}
          fi

      - name: Build and Test on Windows
        if: runner.os == 'Windows'
        run: |
          if ($env:matrix_os -eq "windows-latest" -and $env:matrix_target -ne "x86_64-pc-windows-gnu") {
            cross build --release --target $env:matrix_target
            cross test --release --target $env:matrix_target
          } else {
            cargo build --release --target $env:matrix_target
            cargo test --release --target $env:matrix_target
          }

      - name: Run program on Linux
        run: |
          timeout 60s ./target/${{ matrix.target }}/release/ifstat-rs -V
          timeout 60s ./target/${{ matrix.target }}/release/ifstat-rs --version
          timeout 60s ./target/${{ matrix.target }}/release/ifstat-rs -a 0 1
        if: matrix.os == 'ubuntu-latest' && matrix.target == 'x86_64-unknown-linux-gnu'

      - name: Run program on macOS
        run: |
          brew install coreutils
          gtimeout 60s ./target/${{ matrix.target }}/release/ifstat-rs -V
          gtimeout 60s ./target/${{ matrix.target }}/release/ifstat-rs --version
          gtimeout 60s ./target/${{ matrix.target }}/release/ifstat-rs -a 0 1
        if: matrix.os == 'macos-latest' && (matrix.target == 'x86_64-apple-darwin' || matrix.target == 'aarch64-apple-darwin')

      - name: Run program on Windows
        run: |
          Start-Process -FilePath "timeout.exe" -ArgumentList "60" -Wait
          ./target/${{ matrix.target }}/release/ifstat-rs.exe -V
          Start-Process -FilePath "timeout.exe" -ArgumentList "60" -Wait
          ./target/${{ matrix.target }}/release/ifstat-rs.exe --version
          Start-Process -FilePath "timeout.exe" -ArgumentList "60" -Wait
          ./target/${{ matrix.target }}/release/ifstat-rs.exe -a 0 1
        if: matrix.os == 'windows-latest' && (matrix.target == 'x86_64-pc-windows-gnu' || matrix.target == 'aarch64-pc-windows-gnu')
